{% extends 'base.html.twig' %}

{% block title %}CommandProducts{% endblock %}

{% block body %}
    <h1>CommandProducts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ command_product.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ command_product.quantity }}</td>
            </tr>
            <tr>
                <th>TotalPrice</th>
                <td>{{ command_product.totalPrice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_command_products_index') }}">back to list</a>

    <a href="{{ path('app_command_products_edit', {'id': command_product.id}) }}">edit</a>

    {{ include('command_products/_delete_form.html.twig') }}
{% endblock %}
