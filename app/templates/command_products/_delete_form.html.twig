<form method="post" action="{{ path('app_command_products_delete', {'id': command_product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ command_product.id) }}">
    <button class="btn">Delete</button>
</form>
